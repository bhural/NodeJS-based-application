import * as m3o from "@m3o/m3o-node";
export class TunnelService {
    constructor(token) {
        this.client = new m3o.Client({ token: token });
    }
    // Send a request through the tunnel
    send(request) {
        return this.client.call("tunnel", "Send", request);
    }
}
