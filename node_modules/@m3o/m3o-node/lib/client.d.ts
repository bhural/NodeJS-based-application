import { Stream } from './stream';
export interface Options {
    token?: string;
    address?: string;
    prefix?: string;
    local?: boolean;
}
/**
 *
 */
export interface ClientOptions {
    token?: string;
    address: string;
    prefix: string;
    local: boolean;
}
export declare class Client {
    options: ClientOptions;
    constructor(options?: Options);
    call<Request, Response>(service: string, endpoint: string, req?: Request): Promise<Response>;
    stream<Request, Response>(service: string, endpoint: string, msg?: Request): Promise<Stream<Request, Response>>;
}
